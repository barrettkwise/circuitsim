"""
Package for simulating a circuit of logic gates.

Contains:
- `gateobject.py`: Contains the `Gate` class, which represents a logic gate in the circuit.
- `simulator.py`: Contains the `Simulator` class, which runs the simulation of the circuit and updates the states of the gates based on their inputs.
- `parse.py`: Contains functions for parsing a circuit description from a file and creating the corresponding `Circuit` and `Gate` objects.
- `logicops.py`: Contains functions for performing logic operations (AND, OR, NOT, etc.) based on the inputs to the gates.
- `interface.py`: Contains the `Interface` class, which provides a user interface for loading a circuit from a file and running the simulation.
"""
